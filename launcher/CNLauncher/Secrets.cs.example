namespace CNLauncher;

// Copy this file to Secrets.cs (gitignored) and fill in a fine-grained GitHub
// Personal Access Token scoped to ONLY the crystallized-nexus repo, with
// "Contents: Read-only" permission. Needed because the repo is currently
// private; drop this requirement (and this file) once it goes public.
//
// Create one at: https://github.com/settings/personal-access-tokens/new
static class Secrets
{
    public const string GitHubToken = "";
}
